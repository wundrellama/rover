=/  m  (strand ,vase)
;<  our=@p  bind:m  get-our
=/  wire  /rover-content-report
;<  ~  bind:m  (watch wire [our %obelisk] /server)
;<  ~  bind:m  (poke [our %obelisk] %obelisk-action !>([%script %rover %vector "FROM vehicles V SELECT V.label, V.archived; FROM odometer-observations O SELECT O.value-digits, O.decimal-places, O.unit; FROM energy-definitions E SELECT E.label, E.physical-kind, E.quantity-unit, E.archived; FROM vehicle-energy-definitions L SELECT L.archived; FROM vehicle-default-energy-definitions D JOIN energy-definitions E ON D.energy-definition-id = E.energy-definition-id SELECT E.label AS default-energy; FROM energy-acquisitions A SELECT A.observed-start, A.observed-end, A.observed-precision, A.source-zone; FROM fuel-fills F SELECT F.quantity-milli, F.quantity-unit, F.unit-price-mills, F.currency, F.settlement-mode; FROM charging-sessions C JOIN energy-acquisitions A ON C.acquisition-id = A.acquisition-id SELECT A.observed-start AS charging-start; FROM places P SELECT P.label, P.archived; FROM stations S SELECT S.label, S.station-kind, S.archived; FROM energy-acquisition-stations L JOIN stations S ON L.station-id = S.station-id SELECT S.label AS linked-station; FROM energy-definition-subtypes S SELECT S.label, S.archived; FROM energy-subtype-octane O SELECT O.rating, O.method; FROM energy-subtype-blend B SELECT B.blend-kind, B.percent-digits, B.percent-decimals; FROM energy-subtype-grade-code G SELECT G.code; FROM vehicle-default-energy-subtype D SELECT D.recorded-at; FROM fuel-fill-subtype L SELECT L.acquisition-id; FROM fuel-fill-odometers L JOIN odometer-observations O ON L.odometer-id = O.odometer-id SELECT O.value-digits AS linked-odometer; FROM additive-definitions D SELECT D.label, D.archived; FROM fuel-fill-additives L JOIN additive-definitions D ON L.additive-id = D.additive-id SELECT D.label AS linked-additive; FROM economy-breaks B SELECT B.reason; FROM charging-energy-measurements M SELECT M.quantity, M.decimals, M.measure-unit, M.point, M.evidence; FROM battery-observations B SELECT B.measure, B.observed-start, B.observed-end, B.observed-precision, B.source-zone; FROM battery-observation-percent P SELECT P.value-digits, P.value-decimals; FROM battery-observation-segments S SELECT S.filled, S.total; FROM charging-session-batteries B SELECT B.endpoint; FROM charging-efficiency-breaks B SELECT B.reason; FROM charging-costs C SELECT C.cost-state, C.currency; FROM charging-cost-components C SELECT C.component, C.amount-mills; FROM charging-cost-source-totals T SELECT T.total-mills; FROM consumption-observations C SELECT C.value-digits, C.value-decimals, C.consumption-unit, C.scope, C.source; FROM place-addresses A SELECT A.source; FROM place-address-formatted F SELECT F.formatted; FROM place-address-parts A SELECT A.part, A.value; FROM place-coordinates C SELECT C.latitude-scaled, C.longitude-scaled, C.coord-scale, C.source; FROM place-coordinate-accuracy A SELECT A.radius-digits, A.radius-decimals, A.radius-unit; FROM station-brand-operator B SELECT B.role, B.label; FROM station-identifiers I SELECT I.provider; FROM acquisition-station-equipment E SELECT E.equipment-label, E.receipt-text;"]))
;<  [mark =vase]  bind:m  (take-fact wire)
;<  ~  bind:m  (take-kick wire)
(pure:m vase)
